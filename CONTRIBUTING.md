# 운영 규칙

- 원본 미디어는 삭제하거나 덮어쓰지 않고 새 파일로 버전 관리한다.
- 실제 파일 속성이 문서 기준과 다르면 `pending`으로 표시한다.
- 새 산출물은 `assets/`에 역할별로 보관하고 `evidence/manifest.toml`을 갱신한다.
- 미디어를 추가·교체하면 README, 납품 규격, 자산 역할표의 경로·실측값을 함께 갱신한다.
- 검증과 테스트를 통과한 변경만 `main`에 직접 반영한다.
